class Player:

    def __init__(self, name, choice):
        self.name = name
        self.choice = choice

    def make_choice():
        from random import choice
        return choice(["rock", "paper", "scissors"])